# Example promoted artifact record for a Platform target instance.
# Real values belong in the private Core Ops repo.
# Prefer a commit-derived tag over `latest` for explicit promotions.

PLATFORM_IMAGE=ghcr.io/startup-suite/core:abc1234
PROMOTION_CHANNEL=main
PROMOTION_REF=abc1234
