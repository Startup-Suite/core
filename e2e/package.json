{
  "name": "core-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end browser tests for Startup Suite core. Run against a local dev server.",
  "scripts": {
    "install-browsers": "playwright install chromium chromium-headless-shell",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:meeting": "playwright test tests/meeting_av.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^25.6.0",
    "typescript": "^5.7.0"
  }
}
